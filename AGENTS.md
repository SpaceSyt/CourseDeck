# 同步与信息完整性

- 作业是同步基础，不是抓取上限。能确定性读取的课程材料、公告和正文也自动同步，存入独立本地资料库；不把所有资料变成待办。
- AI 只在用户使用 Chat 时调用，使用本地资料及可核验来源回答；未知、冲突和抓取范围不能被模型猜测替代。资料查看和自动同步不依赖配置模型。
- 不漏信息是本项目的核心验收标准。不得把同步成功、登录成功或读取到部分记录等同于信息完整。
- 准确性同时要求不遗漏、不重复制造任务、不猜测字段。明确带截止时间的课程内容必须保留在待办；资料整理和课程合并不得丢失任务身份、截止时间、来源状态或本地笔记。
- 排查来源连接和同步异常时，优先操作该来源的专用浏览器，核对用户实际可见的课程、列表和详情；必要时使用可见窗口与用户对照，避免仅凭后台解析结果作出判断。
- 同步验证必须检查课程覆盖、分页、折叠内容、当前与历史项目，以及正文、截止时间和状态等关键字段。无法覆盖的范围必须明确记录并在影响使用时显示。
- 部分读取、超时、权限限制、解析失败和真实空列表必须区分。失败时保留缓存，不得把未读到的数据当作删除、完成或无需关注。
- 不得静默忽略无法分类或无法解析的内容。过滤、自动忽略与本地删除必须可查看和恢复；分类不确定时保留原内容。
- 修复连接器后，使用真实页面核对结果，并为发现的遗漏场景补充有意义的回归验证。未经核对，不宣称已完整同步。
- 使用专用浏览器已有登录会话；密码和验证码只在来源登录页面输入，不输出或写入日志。认证参数不得进入公开链接或普通调试文件。

# 界面与文案

- 保持精简的工具型界面，文字用于识别内容、操作、决策或理解异常。
- 不新增宣传语、重复说明或显而易见的操作提示；同步异常、数据缺失和覆盖范围不得为了精简而隐藏。
- 优先修改已有组件和样式，清理本次改动造成的重复或失效代码，不进行无关重构。
